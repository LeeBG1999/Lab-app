import { Route } from '../../../../../../../routes';
import MenuStartPresentations from '../views';
import React from 'react';

const MenuStartController: React.FC<{ routes: Route[] }> = ({ routes }) => {
    return null;
    // <MenuStartPresentations
    // 	routes={navRoutes}
    // ></MenuStartPresentations>
};
export default MenuStartController;
