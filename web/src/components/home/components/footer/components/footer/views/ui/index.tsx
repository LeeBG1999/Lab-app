import dat from '../../asset/dat.png';
import styled from 'styled-components/macro';

export const Footer = styled.div`
    position: relative;
    background-image: url(${dat});
    background-size: cover;
    height: 22em;
`;
