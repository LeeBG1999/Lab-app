import React from 'react';
// import { useSignUpMutation } from 'generated/apollo-react-hook.generated';
// import { SignUpView, OnRegister } from './views/SignUpView';
// import EnterActiveOtpComponent from 'components/auth/enter-active-otp';
// import * as firebase from 'firebase/app';

export const SignUpController: React.FC = () => {
    // const course: number[] = [];
    // const [register, { loading, error, data }] = useSignUpMutation();
    // let email = '';
    // const isSignupSuccess = data && data.signUp.__typename === 'Account';
    // if (data && data.signUp.__typename === 'Account') {
    //     email = data.signUp.email;
    // }

    // for (let i = 50; i < 65; i++) {
    //     course.push(i);
    // }

    // const handleRegister: OnRegister = credentials => {
    //     firebase
    //         .auth()
    //         .createUserWithEmailAndPassword(
    //             credentials.email,
    //             credentials.password,
    //         );
    //     // register({ variables: { data: credentials } });
    // };

    // return (
    //     <>
    //         {isSignupSuccess && <EnterActiveOtpComponent email={email} />}
    //         <SignUpView
    //             loading={loading}
    //             courses={course}
    //             error={error}
    //             result={data}
    //             onRegister={handleRegister}
    //         />
    //     </>
    // );
    return <div></div>;
};
