import React from 'react';
import {{view (pascalCase name)}} from './{{view (pascalCase name)}}';

const {{controller (pascalCase name)}}: React.FC = () => {
return (
<{{view (pascalCase name)}} />
);
};

export default {{controller (pascalCase name)}};